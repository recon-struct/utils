{
  "name": "@recon-struct/utils",
  "version": "1.2.1",
  "description": "A collection of utility functions",
  "type": "module",
  "devDependencies": {
    "@recon-struct/utility-types": "^1.4.1",
    "@types/bun": "^1.1.3",
    "prettier": "^3.2.5",
    "prettier-plugin-organize-imports": "^3.2.4",
    "rimraf": "^5.0.7",
    "typedoc": "^0.25.13"
  },
  "license": "MIT",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "generate-docs": "typedoc --out docs src",
    "test": "bun test",
    "dev": "bun build --watch",
    "build": "rimraf ./dist && tsc && bun build.ts",
    "arity-test": "bun --watch src/examples/arity/index.ts"
  },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ]
}
